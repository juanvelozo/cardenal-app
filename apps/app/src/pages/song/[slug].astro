---
import Layout from '@/layouts/Layout.astro';
import { SongViewerContainer } from '@/modules/editor/ui/SongViewerContainer';

const { slug } = Astro.params;
---

<Layout title={`${slug} — Cardenal`}>
  <SongViewerContainer client:load draftId={slug!} />
</Layout>
