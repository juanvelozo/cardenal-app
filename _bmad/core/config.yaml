# CORE Module Configuration
# Generated by BMAD installer
# Version: 6.0.4
# Date: 2026-03-09T13:33:59.432Z

user_name: Juan
communication_language: Spanish
document_output_language: Spanish
output_folder: "{project-root}/_bmad-output"

# Project rules
rules:
  - Hacer un commit por cada funcionalidad. Si hay varias funcionalidades dentro de un mismo archivo, hacer varios commits separados, uno por funcionalidad.
  - "Estrategia de ramas: rama por proyecto (app, landing), rama por funcionalidad usando carpetas (editor/chord-parser, editor/undo-redo). Cada feature branch sale de la anterior formando una escalera lineal (no enredadera). Merge con fast-forward subiendo la escalera. Cuando el módulo esté completo, mergear a develop, borrar la rama del módulo y liberar el namespace para carpetas."
  - "Siempre pushear la feature branch al remote cuando esté terminada, para que quede en el historial de git."
