services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: cardenal
      POSTGRES_PASSWORD: cardenal
      POSTGRES_DB: cardenal_dev
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
